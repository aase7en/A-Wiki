---
name: a-wiki-skill-architecture
description: A-Wiki's universal 5-layer skill architecture that lets every AI agent (Codex/Claude/ZCode/Hermes/Kilo/Cline/Windsurf/OpenClaw/future) see one "skill brain" via a single-source-of-truth registry, with enforced no-drift guarantees.
domain: [engineering, ai-ops]
lifecycle_phase: meta
category: wiki
source: repo
path: wiki/entities/ai-tools/a-wiki-skill-architecture.md
---

# A-Wiki Universal Skill Architecture

[verified 2026-07-01]

> **Tagline:** One registry, every agent. Edit the registry, regen, commit — every agent surface updates automatically.

## Problem solved

A-Wiki grew skills across 6+ locations (skills/, agent-skills/, ~/.claude/, ~/.kilo/, ~/.codex/) with two incompatible discovery paradigms (explicit manifests vs recursive auto-discover) and no enforcement against drift. Result: agents saw different skills, adding an agent required manual multi-file edits, and duplicates accumulated.

## Solution: 5-layer contract architecture

```
LAYER 0: SKILL.md files (content) — skills/ · agent-skills/ · ~/.claude/ · ~/.kilo/ · ~/.codex/
LAYER 1: skills-registry.json  ← SINGLE SOURCE OF TRUTH (name, aliases, domain, lifecycle, status)
LAYER 2: Generators → per-agent surfaces (.kilo kilo.jsonc, .gemini settings.json, AGENTS.md tables)
LAYER 3: Agents load surfaces natively
LAYER 4: Enforcement (PreToolUse hook + pre-commit + CI)
```

**Key principle:** The registry is the only file agents edit for skill management. Generated surfaces are NEVER hand-edited — they're regenerated by `scripts/regen-skill-surfaces.py`.

## How to add a new agent (future-proofing)

Adding a new agent requires exactly 2 changes:
1. Add a generator module in `scripts/skills_registry/generators/gen_<agent>.py` exposing `render(registry)` + `filename`.
2. Add one line to the `GENERATORS` dict in `scripts/regen-skill-surfaces.py`.

Nothing else changes. The registry, drift detection, hook, CI, and all other agents' surfaces continue working.

## How to add a new skill

1. Create `skills/<category>/<skill-name>/SKILL.md` with frontmatter (`name`, `description`, `domain`, `lifecycle_phase`).
2. Add an entry to `skills-registry.json` (or run `regen-skill-surfaces.py --bootstrap`).
3. Run `python scripts/regen-skill-surfaces.py` to update all surfaces.
4. Commit. The `check_skill_registry.py` hook will warn if frontmatter is incomplete.

## How to deduplicate skills

See `docs/protocols/skill-consolidation.md`. Summary:
- **True duplicate** (same content, different paths): keep canonical, record mirrors in `mirror_paths[]`.
- **Thin stub** (re-route entrypoint): mark `status: alias` + `canonical: <name>`.
- **Deprecated**: mark `status: deprecated` + `migrated_to: <name>`.
- **Near-duplicate** (same purpose, different framework): keep content, alias the surface.

The consolidation tool (`scripts/skills_registry/consolidate.py`) is idempotent — safe to run repeatedly.

## Key files

| File | Purpose |
|------|---------|
| `skills-registry.json` | Single source of truth (Layer 1) |
| `scripts/skills_registry/__init__.py` | Schema + Registry + validation |
| `scripts/skills_registry/scan.py` | Bootstrap scanner + domain heuristics |
| `scripts/skills_registry/dedup.py` | Duplicate cluster detection |
| `scripts/skills_registry/consolidate.py` | Consolidation action applier |
| `scripts/skills_registry/generators/` | Per-agent surface generators (Layer 2) |
| `scripts/regen-skill-surfaces.py` | Orchestrator (regen/check/bootstrap/validate) |
| `scripts/verify-skill-surfaces.py` | Cross-agent visibility smoke test |
| `scripts/hooks/check_skill_registry.py` | PreToolUse enforcement gate (Layer 4) |
| `docs/architecture/skill-architecture-plan.md` | Full design doc |
| `docs/protocols/skill-frontmatter-schema.md` | Frontmatter schema reference |
| `docs/protocols/skill-consolidation.md` | Dedup convention |
| `docs/protocols/agent-private-storage.md` | Per-agent drive/.agents/ convention |

## Current state (2026-07-01)

- **331 skills** in registry (325 canonical, 5 alias, 1 deprecated)
- **22 domains** (code, debug, design, ux-ui, trader, medical, business, data, engineering, security, ai-ops, productivity, wiki, iot, env, pharmacy, thai, logistics, network, media, document, sre)
- **8 lifecycle phases** (define, plan, build, verify, review, ship, meta, none)
- **3 generated surfaces** (Kilo paths, Gemini skills map, AGENTS.md index)
- **1 enforcement hook** (check_skill_registry.py) + pre-commit + CI
- **0 drift** (enforced)

## Verification commands

```bash
python scripts/verify-skill-surfaces.py          # cross-agent visibility
python scripts/regen-skill-surfaces.py --check   # drift check (CI mode)
python scripts/regen-skill-surfaces.py --validate # schema validation
python -m pytest tests/test_skills_registry.py tests/test_check_skill_registry.py tests/test_cross_agent_visibility.py
```

## Related

- [[skill-frontmatter-schema]]
- [[skill-consolidation]]
- [[agent-private-storage]]
- [[awiki-lifecycle-router]]
- [[skill-architecture-plan]]
