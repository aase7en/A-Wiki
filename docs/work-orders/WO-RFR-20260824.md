# WO-RFR-20260824 — Post-review hardening + cross-agent engineering loop

Status: READY_FOR_EXECUTION
Owner: capability-routed; scope follows findings, not vendor identity
Created: 2026-08-24
Source review: Senior Staff review at main `14019f98`

## 1. Objective

Close the outstanding post-review findings R-FR-001..011 without relying on chat memory, while making the execution path resumable across Codex, GPT Work, GLM/ZCode, Claude, Gemini, or another capable agent.

The repository is the source of truth. Chat is transport only.

## 2. Binding SSoT rule

Before any implementation or review, read in this order:

1. `AGENTS.md`
2. `COLLAB.md`
3. this work order
4. `docs/migration/awiki-vnext-plan.md`
5. latest checkpoint in this file and the current Git branch/HEAD
6. relevant code/tests for the finding being worked

Never infer current state from a prior chat answer. If repo state disagrees with chat, repo wins and the discrepancy must be recorded here.

## 3. Full loop-engineer cycle — mandatory per remediation slice

Each finding or tightly coupled finding group must complete the whole loop before it is marked done:

`Grill Me / Grill Me with docs -> Brainstorm -> Plan -> Implement -> Review -> Debug -> Test/E2E -> Report -> Memory -> Open PR -> CI/CD -> Fetch merge state`

### Stage requirements

1. **Grill Me / Grill Me with docs**
   - Challenge the requested fix, assumptions, trust boundaries, failure modes, and done criteria before coding.
   - For `grill-with-docs`, verify the local skill against its upstream GitHub source when network access is available. Record upstream URL/ref/date used.
   - If upstream freshness cannot be checked, say `UPSTREAM_UNVERIFIED` and use the pinned local copy; never claim it is up to date.
2. **Brainstorm**
   - Generate at least two viable approaches when design risk is non-trivial.
   - If the current CLI supports subagents, fan out independent design/security/test perspectives. Subagent output is advisory; the primary agent owns the decision.
3. **Plan mode / deep reasoning**
   - Define root cause, invariants, files, tests-first sequence, rollback, and exact acceptance evidence.
   - Keep work micro-stepped and independently resumable.
4. **Implement**
   - Follow Iron Law test-first for production changes.
   - One coherent finding group per branch/PR unless coupling is proven in the plan.
5. **Review**
   - Use an independent reviewer or stronger reasoning mode where available.
   - Review code, security, tests, integration, and regression surface; approval is pinned to exact SHA.
6. **Debug**
   - Any failure requires root-cause isolation before patching. Do not weaken tests to get green.
7. **Test / E2E**
   - Run targeted unit tests plus realistic end-to-end/adversarial usage that crosses the real integration path.
   - Distinguish transport/tool failure from product/test failure.
8. **Report**
   - Record commands, decisive outputs, changed files, residual risks, and reviewer verdict in this work order or the canonical review artifact.
9. **Memory**
   - Persist reusable defect-prevention lessons in the repository's approved durable memory/knowledge mechanism. Do not store secrets/private paths. The lesson must describe how to prevent recurrence, not merely restate the bug.
10. **Open PR**
    - Push the remediation branch and open a draft PR to `main` before independent final review when CI evidence depends on PR events.
11. **CI/CD**
    - Required CI must pass at the reviewed PR HEAD. Never treat an unrelated workflow run as evidence.
12. **Fetch merge state**
    - Reviewer never silently assumes merge. After the human/authorized merge gate, fetch `origin`, verify the PR merge/resulting `main` SHA, then update this work order/checkpoint. If not merged, state `AWAITING_MERGE`.

## 4. Finding routing

Model names below are recommendations, not architecture dependencies. If unavailable, route by capability: high-risk security/integrity work -> strongest independent reasoner; deterministic/mechanical work -> capable lower-cost executor; final review -> independent strongest reviewer.

| Group | Findings | Recommended owner | Reasoning level |
|---|---|---|---|
| A | R-FR-002 live-docs trust boundary | GPT-5.6 Sol Ultra / strongest security-capable reviewer-executor | UltraHigh |
| B | R-FR-006 + R-FR-007 adopt merge + exact check | GPT-5.6 Sol Ultra | UltraHigh |
| C | R-FR-001 skill eval->approve artifact binding | GPT-5.6 Sol Ultra | UltraHigh |
| D | R-FR-003 stale-spec CI/history/path semantics | GPT-5.6 Sol MAX | HighMax |
| E | R-FR-004 + R-FR-005 fold-back file matching/integrity | GPT-5.6 Sol MAX | HighMax |
| F | R-FR-008..011 minor test/order/doctor/Markdown consistency | GLM 5.3 or GPT-5.6 Sol MAX | High |
| Final | independent adversarial re-review of all closed findings | GPT-5.6 Sol Ultra / strongest independent reviewer | UltraHigh |

## 5. Outstanding findings and required direction

- **R-FR-001 major — skill pipeline artifact binding**: approve must install the exact evaluated artifact; bind eval to content hash/version and reject post-eval tampering; add real default-apply/tamper E2E.
- **R-FR-002 major — live docs trust boundary**: HTTPS trust policy, reject local/private/reserved targets and `file://`, revalidate redirects, response byte/content/deadline limits, cache-I/O failure containment, adversarial tests.
- **R-FR-003 major — stale-spec CI**: do not depend on unavailable shallow history or wall-clock commit timestamps; fail closed on unknown history; normalize/validate repo-relative paths including cross-platform forms.
- **R-FR-004 major — plan fold-back matching**: populate changed-file paths and structurally match normalized files to plan scopes instead of summary/tag text only.
- **R-FR-005 major — plan fold-back integrity**: normalize/escape untrusted summary/session data before Markdown insertion and use a collision-resistant stable entry ID/hash.
- **R-FR-006 major — adopt provider config preservation**: deep-merge A-Wiki-owned hook entries without deleting foreign hooks; define conflicts and test every provider.
- **R-FR-007 major — adopt `--check` exactness**: compare canonical A-Wiki-owned wiring fields, not mere key/JSON presence.
- **R-FR-008 minor — claims-isolation E2E**: enter through the real hook runner; do not manually inject the store env that the runner is supposed to derive.
- **R-FR-009 minor — workspace env ordering**: normalize provider payload first, then export workspace env once from canonical payload.
- **R-FR-010 minor — doctor CI evidence**: query the intended Core CI workflow for the current HEAD/ref, not the latest arbitrary workflow run.
- **R-FR-011 minor — Markdown link/code consistency**: use one Markdown-aware stripping/tokenization path for link/orphan analysis and cover multi-backtick/fence variants.

## 6. Execution order

1. **M1 Baseline evidence** — run minimum bar before remediation and record result.
2. **M2 Group A** — R-FR-002.
3. **M3 Group B** — R-FR-006/007.
4. **M4 Group C** — R-FR-001.
5. **M5 Group D** — R-FR-003.
6. **M6 Group E** — R-FR-004/005.
7. **M7 Group F** — R-FR-008..011.
8. **M8 Final regression + independent Ultra review + PR/CI + merge verification.**

Do not start the next high-risk group while the current group's root cause or acceptance evidence is unresolved.

## 7. Minimum bar and evidence contract

Baseline/final minimum bar:

```bash
python -m pytest tests/ -q
python scripts/awiki-doctor.py --full
python scripts/health/wiki_health.py
```

For each finding, add targeted tests that fail before the fix and pass after it. For every claim marked VERIFIED, record the exact command and decisive output. If a command cannot run because of MCP/transport/tool failure, mark it `UNVERIFIED — transport/tool failure`; do not convert that into a product failure or a pass.

## 8. Prompt — Codex implementation/review worker

Recommended effort: **HighMax** for deterministic slices; **UltraHigh** if Codex is assigned a security/integrity slice.

```text
PROJECT IDENTITY — BINDING
Project name: A-Wiki
Local project root (Windows): <LOCAL_PROJECT_ROOT>
GitHub repository: https://github.com/aase7en/A-Wiki
Base branch: main
Governance/work-order branch: docs/rfr-remediation-routing-20260824
Governance draft PR: #26
Current work-order SSoT: docs/work-orders/WO-RFR-20260824.md
Assigned finding group: <FILL EXACT R-FR ID(S) BEFORE SENDING>

If you are running in a local Windows checkout, work only from <LOCAL_PROJECT_ROOT>.
If the environment mounts the repo at another path, first prove that `git remote get-url origin` resolves to https://github.com/aase7en/A-Wiki (or its SSH-equivalent) and that the checkout is this repository. Never substitute another A-Wiki-related repo such as A-Wiki-Conductor.

The repository is the SSoT. Do not use chat memory as source of truth.

ENTRY GATE:
1) cd/open the verified A-Wiki checkout above.
2) Read, in order: AGENTS.md -> COLLAB.md -> docs/work-orders/WO-RFR-20260824.md -> docs/migration/awiki-vnext-plan.md -> latest checkpoint in this work order.
3) Verify and record: `git rev-parse --show-toplevel`, `git remote get-url origin`, branch, HEAD, `git status --short`, active claims, and latest checkpoint.
4) If the governance PR has already merged, fetch origin/main and follow the work order from its latest checkpoint; do not revive this governance branch merely because it is named here.
5) Work only on the exact finding group assigned above. Do not start another group.

Run the complete loop-engineer cycle:
Grill Me / Grill Me with docs (verify relevant upstream GitHub source/ref freshness; otherwise label UPSTREAM_UNVERIFIED)
-> brainstorm (use subagents if this Codex CLI supports them)
-> deep plan
-> test-first implement
-> independent review
-> root-cause debug
-> realistic E2E/adversarial tests
-> evidence report
-> durable defect-prevention memory
-> open/update the correct draft PR
-> wait for/inspect required CI at the exact PR HEAD
-> after authorized merge, fetch origin and verify resulting main SHA.

Never weaken a test to make CI green. Never say VERIFIED unless you ran/read the evidence yourself. Keep changes micro-stepped, public-safe, and resumable. Update docs/work-orders/WO-RFR-20260824.md before stopping or handing off.
```

## 9. Prompt — GPT Work deep implementation / final review

Recommended effort: **UltraHigh** for R-FR-002, R-FR-006/007, R-FR-001, and final re-review; **HighMax** for R-FR-003/004/005 if Ultra is unnecessary.

```text
PROJECT IDENTITY — BINDING
Project name: A-Wiki
Owner/repository: aase7en/A-Wiki
GitHub repository: https://github.com/aase7en/A-Wiki
Canonical local path on the owner's Windows machine: <LOCAL_PROJECT_ROOT>
Base branch: main
Governance/work-order branch: docs/rfr-remediation-routing-20260824
Governance draft PR: #26
Current work-order SSoT: docs/work-orders/WO-RFR-20260824.md
Assigned finding group: <FILL EXACT R-FR ID(S) BEFORE SENDING>
Task mode: <IMPLEMENTATION | INDEPENDENT_REVIEW | FINAL_REREVIEW>

Act as Senior Staff Engineer + security auditor + test engineer for THIS A-Wiki repository only.
Do not work on A-Wiki-Conductor or another similarly named repository.
If GPT Work has a mounted checkout instead of the Windows path, identify the checkout whose origin is https://github.com/aase7en/A-Wiki and prove repo identity before reading/mutating files.
The repo is the SSoT; ignore prior chat claims unless the repo independently proves them.

ENTRY GATE
1) Open the verified aase7en/A-Wiki checkout.
2) Read, in order: AGENTS.md -> COLLAB.md -> docs/work-orders/WO-RFR-20260824.md -> docs/migration/awiki-vnext-plan.md -> latest checkpoint.
3) Verify repo root/origin/branch/HEAD/status/claims before mutation or review.
4) If PR #26 has merged, fetch origin/main and continue from the latest work-order checkpoint. Do not assume the governance branch remains the implementation branch.
5) Operate only on the Assigned finding group above.

For the assigned R-FR finding group, run the FULL engineering loop:
1. Grill the requirement and, when docs/skills are used, verify the relevant upstream GitHub source/ref is current; explicitly mark any freshness you cannot verify.
2. Brainstorm at least two approaches; use subagents/personas when the Work environment supports them.
3. Deep-plan invariants, abuse cases, tests-first sequence, rollback and acceptance evidence.
4. Implement the smallest safe fix with failing tests first when Task mode=IMPLEMENTATION. For review modes, do not mutate production code unless explicitly authorized by the work order.
5. Perform an independent-style code/security/test review pinned to the exact SHA.
6. Debug root causes; never patch around failing assertions.
7. Run targeted + realistic end-to-end/adversarial tests through the real integration path.
8. Persist an evidence report and reusable defect-prevention lesson in approved repo SSoT/memory.
9. Open/update the appropriate draft PR to main; require the correct CI for that exact PR HEAD.
10. Do not merge as reviewer. After the authorized merge gate, fetch origin and verify the merged main SHA, then update docs/work-orders/WO-RFR-20260824.md.

Required output: project identity proved, task mode, assigned finding IDs, finding status (OPEN/RESOLVED/VERIFIED), files changed, exact commands + decisive outputs, residual risk, PR/CI state, and one next action. No unsupported VERIFIED claims.
```

## 10. Checkpoint

### 2026-08-26 — governance PR #26 MERGED (fetch-verified)

- PR #26 merged at main **`f0c3a78e`** (2026-08-26T04:44:31Z), fetch-verified by reviewer (GLM/ZCode) per stage 12.
- Reviewer independently re-ran the PR's claimed validation on branch HEAD `7315de90` (isolated worktree): `tests/test_check_handoff.py` 5 passed · `check-privacy.py` clean · `wiki_health.py` 0 hard/0 baselined.
- Review fix-ups: Loop-Evidence section added to PR body; AGENTS.md "Binding Loop Engineer" cross-referenced to the Universal Loop Contract — single normative loop (`fe80c1d0`).
- Incident + fix: pr-loop-gate workflow checked out `pull_request.head.sha`, so branches predating `scripts/check_pr_loop.py` (like this one) exited 2 instead of running the check. Fixed on main `c140b0d1` (TDD: `test_gate_workflow_checks_out_merge_ref_not_head`, red→green). Gate then passed on this PR at `a68d8fc5` (3/3 checks green).
- All checks at reviewed HEAD `a68d8fc5`: loop-contract pass · py38-smoke pass · Core verification pass.
- **Next executable step: M1 baseline evidence** (`pytest tests/ -q` · `awiki-doctor.py --full` · `health/wiki_health.py` on `f0c3a78e`), then Group A R-FR-002.

### 2026-08-24 — planning/governance setup

- Senior review findings R-FR-001..011 are now captured in repo SSoT.
- Routing principle recorded: Ultra for security/integrity/high-blast-radius groups; MAX for complex deterministic groups; GLM/lower-cost agent for mechanical minors/tests; Ultra independent final review.
- No remediation production code is part of this work-order setup commit.
- Governance branch: `docs/rfr-remediation-routing-20260824`.
- Draft PR: **#26** — governance/SSoT only; independent review/CI applies to this exact PR HEAD before merge.
- Next executable step after governance merge/authorization: **M1 baseline evidence**, then Group A R-FR-002.
- Prompt correction 2026-08-24: both Codex and GPT Work templates now bind the exact project identity (`<LOCAL_PROJECT_ROOT>`, `aase7en/A-Wiki`), base branch, governance branch/PR, SSoT work-order path, assigned finding placeholder, and repo-origin verification so an agent cannot accidentally operate on A-Wiki-Conductor or another similarly named checkout.


### 2026-08-28 — stale branch archaeology reconciled against current main

Purpose: resolve whether three old refs still contain unique work before any merge/cherry-pick/archive action. GLM/ZCode supplied a read-only audit claim; ChatGPT-GPT-5.6-Sol independently re-ran decisive Git/tree/history checks before accepting or correcting it.

- `claude/global-env-system` @ `ba7bfb0a`: **PARTIAL, not fully superseded.** `git cherry main` shows one non-patch-equivalent commit, `00d26b00` (Y1/Y2 prompt logging/eval generation). `scripts/hooks/prompt_redactor.py` and `scripts/eval/prompts_to_suite.py` are byte-identical to current main, and their tests/docs exist. However current main has no `AWIKI_LOG_PROMPTS`, `_maybe_log_prompt`, or `PROMPT_LOG_DIR` producer wiring in `scripts/hooks/log_subagent_result.py` while `prompts_to_suite.py` still instructs operators to set `AWIKI_LOG_PROMPTS=1`.
- History evidence: `5cd15f88` (`auto: session end 2026-07-20`) selectively introduced the redactor, suite generator, tests and docs to main but did not modify `log_subagent_result.py`. GLM/ZCode independently re-audited this on 2026-08-28 and reached the same conclusion after ruling out MOVED and INTENTIONALLY_REMOVED. No later main commit removing `_maybe_log_prompt` was found. **Classification: ACCIDENTALLY_LOST / MISSING producer wiring.** Independent read-only history audit confirmed `00d26b00` is the only main-external implementation of `PROMPT_LOG_DIR` / `_maybe_log_prompt()` / its call-site; `git log -S "_maybe_log_prompt" origin/main` is empty, and no moved implementation or intentional-removal record was found. Implementation status remains OPEN until a fresh TDD/privacy review proves the intended current hook behavior. Do not cherry-pick `00d26b00` wholesale.
- `origin/docs/awiki-continuity-recovery-20260820` @ `7256e9ef`: all 11 commits remain Git-unique (`git cherry` `+`), so this is not patch-equivalent. The two target migration docs exist on main in substantially newer semantic state: Phase 6 merged, phases 7–11 completed, and post-review authority moved to this WO. **Classification: SEMANTICALLY_SUPERSEDED / history-only; no content salvage currently required.**
- `claude/zealous-lovelace-29vm1b` @ `80788189`, PR #1: CLOSED and never merged. Main still lacks its three-file delta (`index-it.md`, PowerShell repair tool, Windows User Profile Service runbook); merge-tree is clean. **Classification: MISSING CANDIDATE, not merge-ready.**
- Zealous correction to the advisory audit: there is no PR comment/review proving why PR #1 was closed. The PowerShell is parameterized rather than intrinsically single-machine, but the original branch/PR contains a personal username/example and was built for a work-PC incident. AGENTS Storage Decision Rule requires machine-specific IT-fix artifacts/private account details to remain in `drive/`; generic reusable wiki knowledge may be promoted only after sanitization and normal review.

Next safe actions: (1) create a bounded TDD slice for the missing Y1 producer after checking current hook/privacy invariants; (2) leave continuity branch closed; (3) defer zealous salvage until private Drive rules are loaded and a public-safe distillation is explicitly scoped. No stale branch should be merged directly.


### 2026-08-29 — Y1 prompt producer restoration (stacked implementation)

- Base: PR #32 head `d11ba0f4`; branch `fix/wo-rfr-y1-prompt-producer`. Claim is limited to this WO/COLLAB plus `scripts/hooks/log_subagent_result.py` and `tests/test_log_subagent_result.py`.
- Root cause is now VERIFIED as `ACCIDENTALLY_LOST`: consumer/redactor landed on main through `5cd15f88`, but the producer wiring from `00d26b00` never did. No move/removal commit exists on main.
- RED evidence before production edit: `tests/test_log_subagent_result.py` = **5 failed, 13 passed**. Failures covered default-off producer existence, opt-in persistence/redaction, main-hook forwarding, best-effort I/O failure, and path-escape defense.
- GREEN implementation is a fresh port, not a cherry-pick: lazy `prompt_redactor` import; `AWIKI_LOG_PROMPTS=1` exact opt-in; local `.tmp/prompts`; safe filename normalization; 2,000-line daily cap; best-effort failure containment; current A/B and substring `HOOK_SKIP` behavior untouched.
- Producer/consumer process E2E: synthetic `Agent` hook subprocess emitted `Y1E2ETest-*.jsonl`; `scripts/eval/prompts_to_suite.py --subagent Y1E2ETest --min-count 1` consumed it and produced one eval case; synthetic file removed immediately after verification.
- Verification: producer/redactor/consumer focused = **32 passed**; hook/subprocess regression = **87 passed**; hook-engine + related suite = **223 passed**; privacy clean; repo security scan = **6,310 tracked / 51 baseline / 0 new**; `gen-index.py --check` pass; `git diff --check` pass.
- PR #32 at `d11ba0f4` independently reached Core CI + loop-contract + py38 success and remains the required parent for this stacked slice until its authorized merge.

Next safe action: commit/push this GREEN implementation, open a stacked draft PR with base `docs/wo-rfr-branch-reconciliation-20260828`, inspect exact remote diff/CI, then require independent exact-SHA privacy/security review before changing its base to main or merging.

### 2026-08-30 - M1 baseline captured + cross-agent relay refresh

- Baseline authority: `origin/main` `71406f8a25079f364face422012e4bdeac5f483e` (PR #33 merge). GitHub A-Wiki Core CI push run `33265956876` = success; scheduled Wiki Health at the same SHA = success.
- Exact local Windows isolated-worktree evidence: `python scripts/health/wiki_health.py` = **0 hard / 352 advisory**. Exact `python scripts/awiki-doctor.py --full` fails because console encoding `cp874` cannot encode its emoji. With `PYTHONIOENCODING=utf-8`, doctor reaches normal checks and only `preflight` is red because the verification checkout is an isolated non-main worktree.
- Exact `python -m pytest tests/ -q` exposes baseline cross-platform/worktree assumptions including direct `.git/HEAD` access, Windows console encoding, and Bash-on-Windows environment behavior. Full exact result: **58 failed / 3398 passed / 19 skipped** in 403.58s. These failures are baseline debt and are not attributed to R-FR-002.
- Safety: the first M1 worktree developed unknown dirty/generated state plus mojibake during text operations and is quarantined without reset/clean/commit. Reconciliation continues on clean branch `docs/wo-rfr-m1-reconcile-20260830` using UTF-8 byte-safe writes.
- Coordination: repository/WO/branch/PR is the cross-agent message bus. Human relay is reduced to a WO pointer only when direct invocation is unavailable. `conductor claim` is the primary claim-row writer to prevent duplicate manual rows.
- Routing evidence checked 2026-08-30: Z.ai/Hugging Face GLM-5.3 data reports Terminal-Bench 3.0 **28.3**, DeepSWE v1.1 **66.9**, AutomationBench v1.0.6 **48.2**. GLM 5.3 MAX is a strong deterministic/long-horizon executor candidate; security/protocol/final review stays with the strongest independent reasoner. Vendor results are routing signals, not universal-superiority proof.
- **Next after this checkpoint PR/CI/merge:** M2 Group A - R-FR-002 live-docs trust boundary. M3 remains blocked until M2 is resolved/verified.
- Final PR #34 re-audit caught two governance-integrity defects before merge: Unicode separators in `WO-LANES-20260824.md` had become literal `?`, and active continuity/COLLAB sections still instructed manual claim writing / detailed human relay. Repaired with UTF-8-safe writes and canonical conductor-primary/no-human-relay wording before final CI.


### 2026-08-30 — M2 R-FR-002 RED + security design

- Base/branch: `origin/main` `1bf0cc73a96830c46e51e1ea64ead20e4de087fc` -> `fix/wo-rfr-m2-live-docs`; isolated worktree, clean before claim. `conductor claim` committed/pushed at `32cd3947`; scope is only COLLAB/this WO/live_docs/tests.
- Upstream grill freshness checked 2026-08-30 against `mattpocock/skills` main: `skills/engineering/grill-with-docs/SKILL.md` currently delegates to the grilling/domain-modeling flow; latest release observed `v1.1.0` (`d574778`). Python `urllib.request` redirect behavior was also checked against current Python docs. This satisfies the upstream-docs freshness step for this slice.
- Root cause: `_default_get()` delegates an attacker-controlled URL to `urllib.request.urlopen()` after no trust validation; redirects and connection-time DNS are therefore outside A-Wiki policy. The response is read unbounded and cache creation/write can raise or leave a partial file.
- Brainstormed approaches:
  1. **Pre-resolve + validate + continue using `urlopen(hostname)`** — small diff but rejected: connection-time hostname resolution can differ from the validated answer (DNS rebinding / TOCTOU), and urllib redirect handling would require separate revalidation hooks.
  2. **Bounded pinned HTTPS transport** — selected: HTTPS-only; parse/validate every target; resolve all answers and fail closed if any address is non-global/loopback/private/link-local/multicast/reserved/unspecified; connect to a validated numeric IP while preserving the original hostname for TLS SNI/certificate verification and Host routing; manually handle/revalidate each redirect; stream under a byte cap and one total deadline; atomic best-effort cache write.
- Contract limits selected for the bounded utility: max 5 redirects, max 2 MiB response body, text-like content only, existing 15-second timeout becomes a total deadline across DNS/connect/redirect/read. No proxy/environment transport is used. No runtime wiring is added because archaeology found no production `fetch_doc()` callsite.
- Injection seam: a supplied `http_get` remains a trusted test/adapter seam; `fetch_doc()` still applies scheme/hostname/literal-IP policy before invoking it. DNS/IP pinning guarantees belong to the default transport, not arbitrary injected transports.
- RED evidence: baseline `python -m pytest tests/test_live_docs.py -q` = **5 passed**. After adversarial tests, same command = **23 failed / 5 passed**. Failures cover non-HTTPS/missing host, blocked literal IPv4/IPv6, blocked/mixed DNS answers, redirect pivot/redirect cap, response-size/content-type limits, total deadline, atomic cache write, and cache-I/O containment.
- Next safe action: implement the selected bounded transport in `scripts/lib/live_docs.py` only, then rerun the adversarial suite without weakening tests.


### 2026-08-30 — M2 implementation GREEN checkpoint

- Production implementation now uses a bounded pinned HTTPS transport: URL syntax/literal policy -> deadline-bounded DNS -> fail-closed validation of every returned IP -> TCP connect to the validated numeric IP -> TLS SNI/certificate verification against the original hostname -> manual redirect handling with target revalidation on every hop.
- Resource limits: 5 redirects, 2 MiB streamed body cap (including Content-Length precheck), text-like content types only, one 15-second total deadline spanning DNS/connect/TLS/redirect/read. Redirect responses are not body-read. Environment proxy handling is intentionally bypassed.
- Cache hardening: same-directory temp file + fsync + `os.replace`; cache directory/write failures are best-effort and do not break a successful live fetch. Cache payloads now carry policy marker `public-https-pinned-v1` and URL identity; legacy pre-hardening caches or URL-mismatched payloads are ignored rather than trusted.
- Review/debug loop found a second-order defect after the first GREEN: a fresh cache produced by the old untrusted transport would have bypassed the new network policy. Added RED tests (`2 failed / 28 passed`), then policy-version/URL binding; final targeted result **30 passed**.
- Live network E2E (real DNS/TLS/HTTP/cache) against the current `mattpocock/skills` raw GitHub `grill-with-docs/SKILL.md`: first fetch `source=live`, second fetch `source=cache`, policy marker present, expected `grill-with-docs` + `domain-modeling` markers present.
- Broader local evidence: `python -m py_compile scripts/lib/live_docs.py`; `pytest tests/test_live_docs.py tests/test_brain_entry_paths.py tests/test_check_handoff.py -q` = **38 passed**; privacy clean; repo security scan **6312 tracked / 51 baseline / 0 new**; wiki health **0 hard / 352 advisory**; `git diff --check` pass.
- Defect memory mechanism: **Tier 1 regression tests** in `tests/test_live_docs.py` prevent recurrence of SSRF target-policy, redirect pivot, DNS mixed-answer/rebinding preconditions, response resource caps, deadline, atomic cache, and legacy-cache trust defects. Prose is supplemental; tests are the durable prevention mechanism.
- Residual risk to carry into independent review: OS DNS resolution is isolated in a daemon worker because Python's blocking `getaddrinfo()` is not cancellable; the caller still returns on the total deadline and no connection occurs after timeout, but a stuck resolver thread can outlive that call. The injected `http_get` seam is trusted by contract and does not inherit DNS/IP pinning guarantees.
- Next safe action: commit/push implementation, run an independent read-only security review pinned to that SHA, remediate any findings with new tests first, then draft PR -> CI -> exact-SHA re-audit -> authorized merge/fetch verify.


### 2026-08-30 — M2 deep self-audit + reviewer-tool checkpoint

- Independent-review attempts were made before PR: `claude ultrareview main --json` returned `Ultrareview is currently unavailable`; Claude print-mode read-only review then hung without output and was terminated; Gemini CLI read-only plan mode failed authentication with `IneligibleTierError / UNSUPPORTED_CLIENT`. These are **UNVERIFIED — reviewer tool failures**, not code passes/failures. No external reviewer verdict is claimed.
- Strongest-available self adversarial review then found two additional URL/IP-class edge classes and converted both into Tier-1 regression tests before patching:
  1. IPv6 transition/translation forms can have misleading `ipaddress` classification across Python versions. Public IPv4-mapped/compatible/NAT64 forms are now fail-closed (tests include `::ffff:8.8.8.8`, IPv4-compatible and NAT64/loopback-shaped examples).
  2. `urllib.parse.urlsplit()` may normalize/remove C0 controls such as CR/LF/TAB before later validation. Raw URL control characters are now rejected *before parsing*, preventing normalization differentials and request/header ambiguity.
- Additional adversarial coverage now verifies URL credentials rejection, DNS resolution returns within the total deadline even when `getaddrinfo()` blocks, connected peer must equal the pinned IP, and declared `Content-Length` cannot exceed the body cap.
- Focused M2 suite after these fixes: `python -m pytest tests/test_live_docs.py -q` = **40 passed**.
- Python 3.8 syntax compatibility checked with `ast.parse(..., feature_version=(3,8))` = PASS. No Python 3.8 runtime is installed on this machine, so runtime compatibility remains for CI/remote evidence rather than a local claim.
- Next safe action: rerun broader local gates + real GitHub Raw E2E on the current working tree, then commit/push, open draft PR, inspect remote diff and exact-SHA CI, perform final self re-audit because independent reviewer services are unavailable, and merge only if all required evidence remains green.

### 2026-08-30 - M2 freeze evidence before PR

- UTF-8 audit found four corrupted separator sequences in earlier M2 checkpoint headings. Repaired with UTF-8-safe Python writes; post-repair scan of all claimed files reports zero known mojibake markers and zero Unicode replacement characters. This is documentation-integrity evidence, not a product-code finding.
- Current adversarial target: `python -m pytest tests/test_live_docs.py -q` = **40 passed**.
- Broader target: `python -m pytest tests/test_live_docs.py tests/test_brain_entry_paths.py tests/test_check_handoff.py -q` = **48 passed**; `python -m py_compile scripts/lib/live_docs.py` = pass.
- Actual Python 3.8 runtime check after installing managed CPython 3.8.20: `python3.8 -m py_compile scripts/lib/live_docs.py` plus import smoke = **PASS** (`PY38_IMPORT_OK public-https-pinned-v1`).
- Public-safety gates: `check-privacy.py` clean; security scan = **6312 tracked / 51 baseline / 0 new**; `git diff --check` = pass.
- Real-network E2E against upstream GitHub Raw on the current tree = `live -> cache`, cache policy `public-https-pinned-v1`, expected upstream markers present.
- Independent reviewer services remain unavailable/tool-failed, so no independent PASS is claimed. PR CI plus exact-SHA self adversarial re-audit remains mandatory before merge.
- Next safe action: commit/push this freeze state, open the M2 draft PR, inspect exact remote diff, wait for exact-SHA CI, re-audit, then merge only under the standing authorization if all gates remain green.

### 2026-08-30 - M2 merged verification + M3 R-FR-006/007 design

- M2 PR #35 was reviewed at exact HEAD `d977312f6660c611ff0390efccbe70126ed19637`; PR Loop Gate, Core verification, and py38-smoke all passed. It merged as `6574bb7c2a9565471e04ad1f9f3769ac9c7be58a`; fetch verification proved the reviewed HEAD is an ancestor of `origin/main` and the hardened live-docs policy markers are present on merged main. M2 is VERIFIED and M3 is unblocked.
- M3 recovery found no open PR, branch, or worktree for R-FR-006/007. Isolated branch `fix/wo-rfr-m3-adopt-integrity` starts from exact merged main `6574bb7c...`; conductor claim `c3a3e648` owns only COLLAB/this WO/`scripts/awiki-adopt.py`/`tests/test_awiki_adopt.py`.
- Upstream grill freshness rechecked 2026-08-30 against `mattpocock/skills` main: `grill-with-docs` still delegates to `grilling` + `domain-modeling` (GitHub modified 2026-08-15).
- Root cause archaeology for R-FR-006: `_install_provider_configs()` replaces the complete provider `hooks` subtree for Claude/Codex and the complete ZCode hooks object, deleting unrelated hooks/config owned by the target repo. `_write_json_if()` also turns invalid JSON into `{}`, which can silently erase foreign configuration.
- Root cause archaeology for R-FR-007: `_verify()` checks Claude only for the existence of `PreToolUse`; ZCode/Codex only need parseable JSON; MCP only needs an `awiki` key. Wrong commands, matcher, timeout, provider/event, duplicates, stale brain path, or wrong MCP args/env can therefore pass `--check`.
- Brainstormed approaches: (A) generic recursive dict/list deep merge - rejected because lists cannot distinguish A-Wiki-owned hooks from foreign hooks and stale A-Wiki entries would accumulate; (B) **owned-entry replacement** - selected: identify A-Wiki hook commands, strip only those from existing blocks while preserving foreign blocks/hooks/top-level fields, then append one canonical A-Wiki sweep per provider. For ZCode preserve unrelated hooks keys/events while A-Wiki owns `enabled=true` and its sweep entries.
- Exact-check invariant: normalize/extract only A-Wiki-owned hook wiring from each provider and compare it exactly with `_sweeps(brain, provider)` while ignoring foreign additions. The `awiki` MCP entry is A-Wiki-owned and must equal the canonical command/args/env exactly; other MCP servers remain allowed.
- Failure policy: existing invalid JSON or wrong managed-container types fail closed and remain byte-identical; adoption must never "repair" malformed foreign config by erasing it.
- GLM-5.3 routing: current official Z.ai/Hugging Face evidence remains strong for deterministic long-horizon coding/test expansion (DeepSWE 66.9, Terminal-Bench 3.0 28.3, AutomationBench 48.2). It is suitable for a bounded archaeology/test-review lane, but the repo `launch-glm` path still maps older defaults and two direct `glm-5.3` unattended pings timed out even with safe mode. Therefore no mutable parallel GLM lane is opened; this is recorded as a dispatch-tool limitation, not a model-quality failure. Human relay is not required for the current M3 execution.
- Test-first plan: preserve foreign hooks/top-level fields for Claude/ZCode/Codex; replace stale/mixed A-Wiki hooks without duplicates; make exact check fail on tampered owned wiring and pass with foreign additions; fail closed/byte-preserve invalid JSON or wrong container types; exact-check MCP A-Wiki entry while preserving foreign servers; then run existing real-brain adopt E2E.

- M3 RED evidence before production mutation: existing `python -m pytest tests/test_awiki_adopt.py -q` = **8 passed**. After the adversarial R-FR-006/007 matrix = **16 failed / 11 passed**. The failures mechanically reproduce foreign-hook deletion across all three providers, stale/mixed A-Wiki hook replacement gaps, false-green tampered `--check`, destructive malformed/wrong-container handling, and false-green MCP wiring drift.

### 2026-08-31 - M3 R-FR-006/007 implementation + deep-audit checkpoint

- Test-first history: original adopt suite **8 passed**; first R-FR-006/007 adversarial expansion reproduced **16 failed / 11 passed** before production edits. Later self-review added preservation/link-boundary cases that independently went RED before their fixes, including foreign empty events and linked provider/seed surfaces.
- R-FR-006 implementation uses owned-entry replacement instead of raw recursive list merge: existing A-Wiki hook commands are removed from mixed blocks, foreign hooks/block metadata/events/top-level provider fields remain, then one canonical sweep is appended for Claude/ZCode/Codex. ZCode keeps foreign hooks metadata while A-Wiki owns `enabled=true` and its sweep.
- R-FR-007 exact verification extracts only A-Wiki-owned hooks and compares matcher/hook command/type/timeout/provider/event structure exactly with the canonical sweep. The `awiki` MCP command/args/env must match exactly while unrelated MCP servers remain allowed. Tampering, duplicates, stale paths, wrong container types, or malformed JSON fail closed.
- Destructive-config prevention: all mutable config surfaces are prevalidated before any write, malformed/wrong-type JSON remains byte-preserved, and late validation failure cannot partially create earlier provider/MCP files.
- Deep audit found an additional contract violation: provider config/parent or seeded files could be symlinked outside the target repo. Added component-wise lstat guards that reject symlinks and Windows reparse points before read/write; `--check` uses the same guard and converts linked AGENTS/provider surfaces to drift instead of following them or raising.
- Real Windows junction E2E: a `.claude` directory junction to an external directory was detected as a reparse point; adopt returned rc=1 and created **no** external `settings.json`.
- Current focused suite: `python -m pytest tests/test_awiki_adopt.py -q` = **38 passed**. Related adopt/brain/hooks/cross-agent sweep = **157 passed / 1 skipped**; three cp874 subprocess warnings are existing Windows encoding debt, not M3 failures.
- Runtime compatibility: managed CPython 3.8.20 `py_compile` + import smoke for `scripts/awiki-adopt.py` = **PASS**.
- Public-safety evidence: privacy clean; security scan = **6312 tracked / 51 baseline / 0 new**; wiki-health = **0 hard / 352 advisory**; `git diff --check` = pass.
- Durable defect prevention: Tier-1 regressions in `tests/test_awiki_adopt.py` cover foreign-hook preservation, stale/duplicate owned hooks, exact-check drift, malformed config byte preservation, all-or-nothing preflight, symlink/reparse escape, and verifier no-follow behavior. No duplicate prose memory file is created.
- Next safe action: remove local temporary audit helpers, run final UTF-8/diff/focused gates, commit/push M3 implementation, open draft PR, inspect remote diff + exact-SHA CI, attempt independent read-only review without granting mutable overlap, then exact-SHA re-audit and authorized merge/fetch verification before M4.

### 2026-08-31 - M3 post-PR adversarial re-audit

- Draft PR #36 opened at `80089b43603cc23ecfbb0f388a7893bcc31a1edc`; GitHub remote patch confirmed exactly four claimed files. PR Loop Gate passed on that SHA; Core CI began, but this SHA is now superseded by later self-review findings and its CI must not be used as merge evidence.
- Independent reviewer attempts remain tool-blocked: `claude ultrareview 36 --json` returned `Ultrareview is currently unavailable`; a no-tools/safe-mode/plan-only Claude print reviewer over the exact PR diff exceeded the bounded tool timeout and its exact spawned process was terminated. These are **UNVERIFIED — reviewer tool failures**, not review PASS/FAIL.
- Exactness self-audit found three additional false-green classes, each converted to RED regression before production change:
  1. extra metadata on an A-Wiki-owned hook block was discarded by normalization, so `--check` could PASS despite altered block semantics;
  2. removing the owned `.tmp/` gitignore rule was not checked, and a comment merely mentioning `.tmp/` could fool install substring logic;
  3. the adopted `AGENTS.md` marker could remain while its owned `Brain root` pointed elsewhere, yet `--check` passed and re-adopt did not repair it.
- Remediation: owned block normalization now preserves all block-level fields except filtering foreign hooks, so unexpected metadata becomes exact-check drift; `.tmp/` uses exact-line semantics in install/check; adopted AGENTS verification requires the exact current Brain-root line, and re-adopt repairs only that owned line while preserving unrelated target text.
- Current focused after these re-audit fixes: `python -m pytest tests/test_awiki_adopt.py -q` = **43 passed**.
- Next safe action: rerun related regressions/Python 3.8/junction/public-safety/UTF-8 on the new tree, commit/push a new exact SHA, update PR #36 evidence, restart exact-SHA CI, then re-audit that final SHA before any merge.

### 2026-08-31 - M3 final pre-push freeze candidate

- Focused adopt integrity suite after all post-PR RED->GREEN findings: **43 passed**.
- Related adopt/brain/hooks/cross-agent regression: **162 passed / 1 skipped**; three Windows cp874 subprocess warnings remain baseline debt outside this slice.
- Managed CPython 3.8.20 compile/import = PASS; privacy clean; security scan = **6312 tracked / 51 baseline / 0 new**; `git diff --check` = pass.
- The previous PR head `80089b43...` is superseded and must not be treated as reviewed/mergeable evidence. Next commit becomes the only valid exact-SHA anchor for PR #36 CI and final re-audit.

### 2026-08-31 - M3 merged verification + M4 R-FR-001 recovery/spec

- M3 PR #36 final reviewed HEAD `8ea4277222397a4916a3c66d7b1faeb749319a39` passed PR Loop Gate + Core CI, merged as `7a9564db3c27e5612db626168aaf8ec19ddb457d`; fetch/ancestor proof and identical tree hashes confirm the reviewed tree is exactly merged main. Post-merge focused adopt suite = **43 passed**, privacy clean. M3 is VERIFIED; M4 is unblocked.
- First M4 worktree `A-Wiki-rfr-m4-skill-artifact-binding` stalled during `git worktree add` with `locked initializing`, zero-byte `index.lock`, near-zero git CPU/I/O, and a partial checkout showing tracked deletes plus duplicate untracked files. No claim or repo mutation was reached. Exact spawned process tree was terminated and that worktree/branch remains quarantined without reset/clean/delete.
- Clean recovery uses sparse isolated worktree `A-Wiki-rfr-m4-skill-artifact-binding-v2`, branch `fix/wo-rfr-m4-skill-artifact-binding-v2`, exact base/main `7a9564db...`. Conductor claim commit `e233f611` owns only COLLAB/this WO/`scripts/lib/skill_pipeline.py`/`scripts/new-skill.py`/their two test modules. Claim was pushed before implementation mutation. Baseline focused tests = **48 passed**.
- Root cause for R-FR-001 is VERIFIED: `run_eval()` evaluates `prop["skill_md"]`, but `_default_apply()` invokes `new-skill.py --apply`, whose `run_scaffold()` renders a new generic TODO `SKILL.md`; therefore the evaluated artifact is never the installed artifact. Proposal/eval also carries no content identity, so editing `skill_md` after eval can still be approved.
- Brainstorm: (A) let pipeline bypass `new-skill.py` and write registry/SKILL directly — rejected as duplicate registry/regen logic; (B) **selected**: content-address the evaluated artifact and add exact-artifact mode to `new-skill.py`, reusing registry-first + regen; (C) scaffold then overwrite — rejected because a wrong artifact lands/regenerates before replacement and partial failure is unsafe.
- Spec/invariants for M4:
  1. `run_eval()` computes SHA-256 over the exact UTF-8 bytes written to evaluator scratch and persists `{sha256,size,encoding}` under eval evidence.
  2. `approve()` requires `status=ready`, a passed eval, and current `skill_md` identity equal to evaluated identity. Drift must not call apply; proposal returns to `draft` with stale-eval evidence so re-evaluation is explicit.
  3. Successful approval persists the approved artifact identity for provenance.
  4. `_default_apply()` transfers exactly `skill_md` through a closed temp file and passes the expected SHA-256 to `new-skill.py`; no generic scaffold content may substitute.
  5. `new-skill.py` exact-artifact mode validates expected hash + frontmatter identity before any registry/SKILL write, derives registry metadata from the artifact frontmatter, preserves normal scaffold mode, writes exact content, then runs existing regen + `--check`.
  6. Tier-1 tests must cover post-eval mutation rejection, exact subprocess handoff, hash/name/frontmatter mismatch fail-closed, byte-for-byte write, registry metadata coherence, and normal scaffold compatibility.
- Routing: fresh Z.ai/Hugging Face evidence still supports GLM-5.3 for bounded deterministic/long-horizon coding, but M4 is integrity-critical and current benchmark signal does not justify overlapping mutable ownership. GPT retains M4 mutation/final audit; GLM is reserved for later non-overlapping mechanical/read-only lanes (especially M7) when dispatch is reliable.
- Next safe action: add the M4 adversarial RED matrix to the two already-claimed test modules, prove failures against `e233f611`, then implement without weakening tests.

### 2026-08-31 - M4 R-FR-001 RED checkpoint

- Production remains unchanged from merged main at this checkpoint. Adversarial tests were added only after the M4 claim was committed/pushed.
- Exact RED result: `python -m pytest tests/test_skill_pipeline.py tests/test_new_skill.py -q` = **10 failed / 48 passed**.
- Failures mechanically prove all target defects: evaluator scratch uses platform newline translation (LF proposal became CRLF bytes on Windows), approve accepts post-eval `skill_md` mutation, approved provenance lacks artifact identity, default apply does not hand the evaluated artifact/hash to `new-skill.py`, and `new-skill.py` has no exact-artifact CLI/config/write path (normal scaffold still owns required domain/phase semantics).
- These tests are the Tier-1 defect-memory mechanism for R-FR-001; implementation must make them green without weakening/removing assertions.
- Next safe action: commit/push this RED checkpoint, then implement content identity + exact-artifact install in the two claimed production files only.

### 2026-08-31 - M4 R-FR-001 implementation GREEN + deep-audit checkpoint

- RED commit: `9ba12a6789f00d0a84847a47ad2547e10540769a` preserved **10 failed / 48 passed** before production edits.
- Implementation: `run_eval()` now hashes and writes the exact UTF-8 bytes it evaluates; eval evidence persists `{sha256,size,encoding}`. `approve()` requires a passed ready eval and exact current artifact identity; post-eval drift is blocked before apply, marked `draft`, and records stale/current identity for explicit re-evaluation. Successful approval records `approved_artifact`.
- Default apply no longer invokes the generic scaffold path. It writes the evaluated artifact into a closed temporary directory, passes `--skill-file` + `--expected-sha256` to `new-skill.py`, and the temp handoff is removed after subprocess completion.
- `new-skill.py` exact mode validates expected SHA-256 and exact frontmatter identity before any repo write, derives registry metadata from the evaluated frontmatter, writes the exact content through the existing registry-first ordering, then reuses regen + `--check`. Ordinary scaffold mode remains supported and still requires explicit domain/phase.
- Deep audit found a second baseline defect: generated pipeline drafts used `domain: [general]`, but `general` is absent from canonical `VALID_DOMAINS`; this could make the old default apply fail even after eval. A separate RED test proved it, then the generic pipeline fallback was changed to canonical `productivity` rather than weakening taxonomy validation.
- Focused result after fixes + CLI exact-mode filesystem E2E test: **60 passed** (`tests/test_skill_pipeline.py tests/test_new_skill.py`). Managed Python 3.8 compile for both production modules = PASS.
- Related registry/hook evidence: `tests/test_check_skill_registry.py` = **7 passed** with `PYTHONUTF8=1`; `tests/test_skills_registry.py -k 'not check_survives_cp874_console'` = **47 passed / 1 deselected**. The deselected console test fails only because this intentionally sparse worktree does not materialize generated `wiki/SKILL-INDEX.md`; three earlier hook failures were the known Thai-Windows cp874 parent-side subprocess decode debt and disappear under UTF-8. Neither is attributed to M4 code.
- Realistic integration E2E: actual pipeline proposal -> real deterministic `run_eval` -> approve -> exact `new-skill` config/scaffold on a temporary filesystem. Installed `SKILL.md` bytes equaled evaluator scratch bytes and proposal UTF-8 bytes exactly; approved/evaluated identities matched; regen + regen-check were both invoked. Evidence: `M4_E2E_OK bbc4f6482572c90bfaa2364a90b767c2e6ce8d608512636d3b3404c913330601 767 2`.
- Durable defect memory is Tier-1 tests, not a duplicate prose memory file: exact eval bytes/hash, post-eval mutation rejection, approved identity, exact subprocess handoff, exact CLI hash/name/frontmatter validation, byte-for-byte install, canonical-domain generation, and normal scaffold compatibility.
- Next safe action: run privacy/security/diff/UTF-8/claimed-scope audit, commit/push the GREEN candidate, attempt independent read-only review pinned to that SHA, then draft PR -> remote diff -> CI -> exact-SHA re-audit -> authorized merge/fetch verification before M5.

### 2026-08-31 - M4 post-merge VERIFIED + M5 R-FR-003 claim/spec

- M4 final reviewed head `4afa6b421e0bac5552d7c6dd9eecd9892622aa5f` merged via PR #38 as `caaf76f176f993fde52823c0a3534c2b30f0ab85`. Fetch/ancestor proof passed and both reviewed/merged trees equal `462bb9097a4f9a057ca1c73c8cb49c562e69c24f`. Main push Core CI #141 succeeded; local post-merge-equivalent focused tests = **60 passed**, privacy clean. M4 is VERIFIED.
- M5 isolated full worktree: `A-Wiki-rfr-m5-stale-spec-integrity`, branch `fix/wo-rfr-m5-stale-spec-integrity`, exact base `caaf76f...`. Conductor claim commit `bfb1a915` owns only COLLAB/this WO/WO-LANES/`scripts/check-stale-specs.py`/`tests/test_stale_specs.py`/`.github/workflows/ci-core.yml`. No M5 branch/claim/open PR existed before creation.
- Root cause: current checker compares `%ct` wall-clock timestamps, maps Git/history failures to empty/zero, skips missing scoped paths, and Core CI uses actions/checkout without `fetch-depth: 0`. Therefore clock skew, shallow history, deletion, or unknown history can false-green.
- Upstream grill (2026-08-31): current Git docs define `A..B` as commits reachable from B excluding those reachable from A; `git rev-parse --is-shallow-repository` exposes shallow state; `--literal-pathspecs` disables glob/pathspec magic. Current actions/checkout code/default remains `fetch-depth=1`; `0` fetches full history.
- Selected design: fail closed if Git history is shallow/unavailable; normalize each exact `scope:` token to canonical repo-relative POSIX form and reject absolute/drive/UNC/`..`; obtain the spec commit OID; use literal-pathspec Git history over `<spec_commit>..HEAD -- <scope>` to detect any post-spec change including deletion; never compare commit timestamps. CI verification checkout must use `fetch-depth: 0`.
- RED matrix: clock-skew commit ordering, shallow clone, Windows separators, absolute/drive/UNC/traversal rejection, missing scoped history, deletion-after-spec, literal metacharacter pathspec, normal fresh/stale compatibility, and workflow full-history assertion. Durable defect memory target = Tier-1 regression tests + Tier-5 CI checkout rule.
- Next safe action: add only the adversarial RED tests first, prove failure against claim head, commit/push RED evidence, then implement the smallest checker/workflow fix.

### 2026-08-31 - M5 R-FR-003 RED checkpoint

- Production remains unchanged from claim/checkpoint head `a24260284a32a2b42266baf5b7cec151f1b774b9`. Baseline before RED expansion: `python -m pytest tests/test_stale_specs.py -q` = **5 passed**.
- Exact adversarial RED result after test-only expansion: `python -m pytest tests/test_stale_specs.py -q` = **14 failed / 5 passed**.
- Failures mechanically reproduce: clock-skew false-green, shallow-history false-green, missing cross-platform path normalizer/validator, missing scoped-history fail-open, deletion-after-spec skip, wildcard/pathspec magic false-positive, non-Git/history failure fail-open, and Core CI checkout without `fetch-depth: 0`.
- The RED suite is the Tier-1 defect-memory mechanism for R-FR-003; implementation must make these assertions green without weakening or removing them.
- Next safe action: commit/push this RED checkpoint before production mutation, then replace timestamp logic with full-history + literal-pathspec revision-range semantics and set Core CI verify checkout to full history.

### 2026-08-31 - M5 R-FR-003 implementation GREEN + deep-audit checkpoint

- RED authority remains `2b5a00ef9fa6c69ab747b1a9de7017ff05e979bc`: **14 failed / 5 passed** before production edits.
- Implementation removes wall-clock comparison entirely. Git subprocess failures/timeouts now raise `HistoryError`; shallow repositories fail closed via `git rev-parse --is-shallow-repository`; `HEAD^{commit}` must resolve.
- Each `scope:` token is normalized to a literal repo-relative POSIX path: separators normalize, absolute/drive/UNC/traversal/control-character forms reject. Git queries run with `--literal-pathspecs`.
- The checker obtains the spec commit OID and asks Git for any scoped-path commit in `<spec_oid>..HEAD`; any such history marks stale. This catches clock skew, deletion, rename, merge-side changes, and change-then-revert without relying on filesystem existence or timestamps. Missing scoped/spec history is UNKNOWN and fails closed.
- Core verification checkout now sets `fetch-depth: 0`; py38-smoke remains shallow because it does not run the stale-spec gate.
- Focused deep-audit suite = **24 passed**. Added second-order coverage for previously false-confidence root-level fixtures, spec-refresh restoring freshness, rename, change/revert, merge DAG, and real CLI shallow failure.
- Related stale/doctor/workflow/hook regression = **194 passed**. `awiki-doctor --full` reports `specs` PASS; its overall isolated-worktree status can remain nonzero for unrelated branch/preflight gates.
- Real A-Wiki full-history E2E: `git rev-parse --is-shallow-repository` = `false`; `python scripts/check-stale-specs.py` = `[OK] ...`, rc=0. Windows `PYTHONIOENCODING=cp874` smoke also rc=0 because checker output is ASCII-safe. Managed Python 3.8 compile = PASS.
- Public-safety/audit: privacy clean; security scan = **6312 tracked / 51 baseline / 0 new**; wiki-health = **0 hard / 352 advisory**; `git diff --check` pass; claimed-file UTF-8 scan has 0 known mojibake/replacement markers; no conflict markers.
- Durable defect memory: Tier-1 `tests/test_stale_specs.py` regressions + Tier-5 Core CI full-history checkout. No duplicate prose memory artifact.
- Independent exact-SHA review still required after candidate commit/push; direct SunDay-Worker MCP is unavailable in this conversation and no reviewer PASS should be inferred from that transport limitation.
- Next safe action: commit/push this GREEN candidate, pin all review/CI to that SHA, open draft PR, audit remote diff, then exact-SHA CI/re-audit/authorized merge/post-merge verification before M6.
### 2026-08-31 - M5 VERIFIED + M6 R-FR-004/005 recovery/spec

- M5 PR #39 reviewed head `f8b8a2ed7f9855a12d49cb7e20e6fc952ee546dd` passed PR Loop Gate + Core CI, merged as `1da8e2ecfd8f14d92f401688364d8ee36f707470`. Fetch/ancestor proof passed; reviewed and merged tree hashes are identical. Push-main Core CI #143 succeeded; local merged-tree-equivalent `tests/test_stale_specs.py` = **24 passed** and real stale-spec checker passed. M5 is VERIFIED.
- M6 isolated worktree/branch starts from exact `1da8e2ec...`; conductor claim `c517964b` owns only COLLAB/this WO/WO-LANES/`scripts/hooks/plan_foldback.py`/`scripts/hooks/memory_capture.py`/`tests/test_stale_specs.py`/`tests/test_memory_capture.py`. No M6 branch/claim/open PR existed before creation.
- R-FR-004 root cause is verified: `MemoryLedger.append()` and `capture_commit()` already support `files[]`, but `memory_capture._detect_changed_files_from_command()` hardcodes `return []`; `plan_foldback._touches()` therefore relies on substring matches over summary/tags instead of structural file evidence.
- R-FR-005 root cause is verified: fold-back inserts raw `session_id` and `summary` into Markdown and deduplicates only by textual `ts=<float>`, allowing Markdown structure injection and collisions between distinct entries sharing a timestamp.
- Selected R-FR-004 shape: derive changed files from the actual successful commit OID using non-shell Git `diff-tree` in the current workspace, normalize/dedupe repo-relative paths, persist them in ledger `files[]`; fold-back normalizes scope/files and, when `files[]` exists, uses exact set intersection only. Legacy entries without file metadata retain a bounded compatibility fallback so old ledger history is not silently discarded.
- Selected R-FR-005 shape: normalize untrusted text to one line, escape Markdown inline metacharacters, compute deterministic SHA-256 over canonical entry identity, append a non-user-controlled fold-back ID marker, and still recognize legacy `(ts=...)` markers for old entries.
- RED matrix: commit producer captures changed files; deletion/rename-safe path capture; matching via normalized Windows/POSIX separators; summary mention must not override mismatching `files[]`; similar-prefix paths must not match; malformed/absolute/traversal file evidence must not match; malicious newline/fence/link/session text cannot alter Markdown structure; same-ts distinct entries both persist; repeat fold-back is hash-idempotent; legacy ts entry remains idempotent; realistic git-commit -> ledger -> plan E2E.
- Multi-agent route: GPT-5.6 Sol owns M6 mutation/final synthesis. Worker5 is verified free fallback but its connector is unavailable in this conversation. Official GLM-5.3 benchmark was refreshed 2026-08-31 and supports a deterministic read-only review lane, but ZCode 0.16.5 headless CLI currently rejects documented options and then reports missing model config; no GLM mutable overlap is opened and no human relay is required.
- Next safe action: run current fold-back/memory-capture baseline, add adversarial tests only, prove RED against `c517964b` + this checkpoint, commit/push RED evidence, then implement the smallest producer/matcher/Markdown-ID fix.

### 2026-08-31 - M6 R-FR-004/005 RED checkpoint

- Production files remain unchanged from M6 checkpoint 7271040c43126a6e0932a7b14415677d28bb98f3; only the two claimed test modules were expanded before this checkpoint.
- Baseline: python -m pytest tests/test_stale_specs.py tests/test_memory_capture.py -q = **36 passed**.
- Exact adversarial RED: same command = **10 failed / 39 passed**. Failures reproduce missing commit-file capture, structural-file fold-back false negatives/positives, invalid evidence fallback, Markdown insertion risk, and same-timestamp identity collision.
- Tier-1 defect memory is the RED regression suite. Assertions must not be weakened to obtain GREEN.
- Next safe action: commit/push this test-only RED checkpoint, then implement only scripts/hooks/memory_capture.py and scripts/hooks/plan_foldback.py.
### 2026-08-31 - M6 R-FR-004/005 GREEN + deep-audit checkpoint

- RED authority: `ae04a12de0c9a0e0768a2af97eb7d705a85c56e2` = **10 failed / 39 passed** before production edits.
- `memory_capture.py` now trusts only the commit OID printed by a successful Git commit result, reads exact changed paths with non-shell `git diff-tree`, normalizes/deduplicates safe repo-relative paths, covers root/rename/delete/merge diffs, and never guesses `HEAD` when the OID/evidence is absent.
- `plan_foldback.py` now treats structural `files[]` as authoritative exact normalized scope evidence. Captured commits with missing file evidence fail closed; bounded prose fallback remains only for legacy/manual non-commit decisions.
- R-FR-005 integrity: untrusted session/summary text is collapsed to one line and Markdown-escaped; deterministic SHA-256 entry markers prevent same-timestamp collisions; exact legacy `(ts=...)` markers remain idempotent without prefix collisions.
- Deep bug hunt also made malformed/non-dict, non-numeric, non-finite, and OS-unrepresentable ledger timestamps skip safely instead of crashing the soft Stop hook. Control-character paths are rejected consistently rather than silently preserved on one platform.
- Focused producer/fold-back suite = **55 passed**. Related memory/ledger/hook/ZCode regression = **211 passed / 5 skipped**. Managed Python 3.8 compile = PASS.
- Realistic E2E: real temporary Git commit -> `memory_capture.main()` -> ledger `files=['src/app.py']` -> plan fold-back -> second fold-back idempotent. Evidence prefix: `M6_E2E_OK`.
- Safety/audit: privacy clean; security scan = **6312 tracked / 51 baseline / 0 new**; wiki-health = **0 hard / 352 advisory**; real stale-spec checker PASS; `git diff --check` PASS.
- Durable defect memory = Tier-1 tests in `tests/test_memory_capture.py` + `tests/test_stale_specs.py`; no new shadow handoff/memory artifact.
- Independent reviewer transports remain separate from correctness evidence: SunDay-Worker connector is not exposed in this chat; ZCode 0.16.5 headless dispatch has CLI/model-config inconsistencies. Attempt exact-SHA Ultrareview after candidate push; unavailable transport must remain UNVERIFIED, never PASS.
- Next safe action: commit/push this GREEN candidate, pin remote diff/review/CI to that SHA, draft PR, exact-SHA re-audit, merge, fetch/post-merge verify, then release M6 and open M7 only after VERIFIED.


### 2026-09-01 - M6 VERIFIED + M7 R-FR-008..011 recovery/spec

- M6 PR #40 reviewed head `a2401b97950f3f387b1acc780c54ae21b4fc5fee` passed PR Loop Gate #32 + Core CI #144 and merged as `bb303a5d870c7253d100b2a1702738e354eea662`. Reviewed and merged trees are both `5511c20b9da3bed335c82fd2241e5b1dc9f1f92e`; post-merge focused tests = **55 passed**, privacy clean, and push-main Core CI run `33396321084` succeeded. M6 is VERIFIED.
- Recovery found the primary local checkout behind `origin/main` with 9 unknown dirty/untracked paths; it remains protected and untouched. M7 therefore uses a fresh isolated worktree/branch from exact `origin/main` `bb303a5d...`, with a clean tree.
- Superseded RFR reconciliation, Y1, and M6 worktrees are clean and each HEAD is an ancestor of `origin/main` with zero commits ahead. Their stale COLLAB ownership rows are released in this governance checkpoint; M7 becomes the sole active R-FR claim.
- M7 conductor claim: `WO-RFR-20260824 M7 R-FR-008..011 mechanical minors`, branch `fix/wo-rfr-m7-mechanical-minors`, limited to the existing WO/COLLAB/lanes plus runner/providers/doctor/Markdown graph-health seams and their targeted tests.
- Worker audit 2026-09-01: no Worker 1-5 is available for reassignment. Worker1/4 serve A-Wiki-Conductor, Worker2 serves pharmacy and has active E2E activity, Worker3 serves env-wastewater, Worker5 serves sunday-estate. No worker is interrupted/rebound.
- Model routing refresh: official Z.ai/Hugging Face GLM-5.3 results remain a good fit for bounded deterministic/automation work (AutomationBench 48.2), while GPT-5.6 Sol remains ahead on Terminal-Bench 3.0 and DeepSWE. GPT retains M7 architecture/integrity/final synthesis; a GLM lane is allowed only if unattended dispatch becomes verifiably usable. ZCode 3.10.2 is live, but its headless execution path is not currently proven.
- F1 R-FR-008/009 root cause: the claims-isolation E2E manually injects `AWIKI_CLAIMS_STORE`, while `hooks_runner.py` exports workspace state before provider normalization. Current Cline payload normalization flattens the provider wrapper and loses outer workspace metadata; current official Cline hook schema exposes top-level `workspaceRoots[]`. Selected fix: canonicalize provider workspace to `cwd`, then export workspace env exactly once from canonical payload; real-runner E2E must prove isolation without an env override.
- F2 R-FR-010 root cause: `awiki-doctor --full` asks `gh run list --limit 1`, so an unrelated latest workflow can be mistaken for Core CI evidence. Selected fix: query the intended Core CI workflow at the exact current HEAD and treat missing/non-success evidence honestly.
- F3 R-FR-011 root cause: graph and health use divergent regex code stripping, while orphan analysis does not strip code at all. Mechanical repro confirms double-backtick spans, tilde fences, and four-backtick fences can create false links/orphans. Selected fix: one lightweight shared Markdown code-region helper used by graph + wikilink/orphan analysis, with CommonMark-compatible delimiter-run/fence tests.
- Dependency graph: F1 (runner/providers) || F2 (doctor) || F3 (Markdown graph/health) are file-disjoint after this shared governance checkpoint and can be delegated in parallel only to independently claimed worktrees. With all Workers occupied and GLM headless unverified, GPT executes them sequentially on the single M7 claim rather than creating overlapping mutable lanes.
- RED plan: F1 real-runner derived claim-store collision + Cline `workspaceRoots` canonical cwd/order; F2 wrong-workflow/latest-run false-positive + exact-HEAD Core query; F3 multi-backtick, >=4-backtick, tilde-fence, and orphan false-link cases. Durable defect memory will be these Tier-1 regression tests, not a new prose memory file.
- Next safe action: commit/push this claim + governance checkpoint before any production mutation, run targeted baseline, run GitNexus impact for each production symbol, then add RED tests only.

### 2026-09-01 - M7 GREEN candidate pre-commit checkpoint

- Base/claim remain `bb303a5d...` / `fix/wo-rfr-m7-mechanical-minors`; claim checkpoint is `1bb0715d...`.
- RED reproduced 6 defects while the existing Claude real-runner coverage passed: Cline workspace isolation, Cline canonical cwd, exact-HEAD Core CI evidence, delimiter-run stripping, code-fence wikilinks, and orphan false inbound links.
- Self-review added one more Tier-1 RED: ambiguous multi-root Cline must fail closed rather than guessing `workspaceRoots[0]`; official Cline hook schema confirms `workspaceRoots` is an array but does not identify an active-root field.
- GREEN implementation: provider normalization derives single-root Cline `cwd`; runner exports workspace state after normalization; doctor pins `ci-core.yml` to exact HEAD; graph + wiki-health share `scripts/lib/markdown_code.py` for backtick spans and backtick/tilde fences.
- Deterministic M7 regression = **214 passed**. Python 3.8.20 no-project compile smoke = PASS; project policy itself is now `requires-python >=3.9`.
- Real evidence: current branch HEAD has no Core CI run and therefore returns no verdict; exact `origin/main bb303a5d...` returns Core CI run `33396321084` SUCCESS. Wiki-health = **0 hard / 352 advisory**.
- Safety: privacy PASS; hook registry = 30 valid (17 hard / 13 soft); stale-spec PASS; security = **6312 tracked / 51 baseline / 0 new**.
- GitNexus fresh local index = 84,948 nodes / 124,324 edges / 701 flows. Pre-edit impact was MEDIUM only for `normalize_payload` (13 impacted); all other M7 symbols were LOW.
- Tool side-effect quarantine: `gitnexus analyze` auto-refreshed only the generated GitNexus blocks in `AGENTS.md` and `CLAUDE.md` at its completion timestamp. Iron Law #5 forbids editing those config files without explicit user permission, so they remain unstaged/protected and are excluded from M7 candidate/PR.
- Worker routing refresh: Worker1/4 remain live on A-Wiki-Conductor; Worker2/3/5 tunnels stopped after recent other-project activity with stdio `OSError [Errno 22]`, so ownership is unresolved and none is rebound.
- Next: stage claimed files only, run GitNexus `detect_changes --scope staged`, diff/security re-audit, commit/push candidate, then obtain independent exact-SHA review if a transport is verifiably available.
