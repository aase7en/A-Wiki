# Phase 16.10 — Release Preflight Patch

> ⛔ **BLOCKER**: All 10 preflight commands must return green before this ticket is marked done.
> Do NOT hand off for demo or sale without a complete evidence bundle.
> Screenshots from the 16.9 smoke matrix are required as part of the bundle.

**Repo**: `<repo-owner>/<product-repo>` · game root: `pixel-wealth-quest/`
**Ticket goal**: Produce one repeatable, auditable evidence bundle suitable for sale/demo handoff — confirming tests, types, build, feed boundary, React health, privacy, roadmap sync, asset balance, and smoke coverage are all green.

---

## Prerequisites

Ensure the dev server is stopped before running build commands. All commands run from the repo root unless noted.

```bash
# Verify you are on main, clean working tree
git status
# Expected: nothing to commit, working tree clean
```

---

## Preflight Run: 10 Commands in Order

### 1. Tests

```bash
npm --prefix pixel-wealth-quest test -- --run
```

**Expected**: All tests pass. Current verified Phase 16.10 count is **679/679** across **116** test files.

If the count drops unexpectedly: check that all chunk test files were committed (run `git log --oneline --since="Phase 16 start date"`) and inspect removed/renamed tests before updating this evidence file.

---

### 2. Typecheck

```bash
npm --prefix pixel-wealth-quest run typecheck
```

**Expected**: 0 errors. TypeScript strict mode must be clean.

If errors appear: do NOT suppress with `@ts-ignore` — fix the underlying type before proceeding.

---

### 3. Build

```bash
npm --prefix pixel-wealth-quest run build
```

**Expected**: `dist/` directory created with no build errors. Verify:

```bash
ls pixel-wealth-quest/dist/
# Should contain: index.html, assets/ folder with hashed bundles
```

If build fails: address Vite/rollup errors before proceeding — a failing build invalidates the evidence bundle.

---

### 4. Feed Boundary Scan (Iron Law — no order/key path)

```bash
npm --prefix pixel-wealth-quest run feed:scan
node pixel-wealth-quest/tools/scan-feed-boundary.mjs
```

**Expected**: 0 violations. The feed boundary scanner must confirm no price feed data, order execution path, or API key literal leaks into client-side bundles.

> This is a non-negotiable Iron Law check (see `docs/protocols/bot-trading-iron-law.md`). Any violation must be fixed before proceeding.

---

### 5. React Doctor

```bash
npx react-doctor@latest pixel-wealth-quest --verbose --scope changed
```

**Expected**: Score **100/100**. Common deductions: missing keys in lists, inline functions in render, component prop drilling beyond 3 levels.

If score < 100: review the `--diff` output, fix each finding, re-run until clean.

---

### 6. Privacy Scan

```bash
python3 scripts/check-privacy.py -v
```

**Expected**: **0 findings** in tracked A-Wiki files. Product repo codenames should be checked by the product-side release audit when a public package is assembled.

If findings appear: do NOT add to a baseline without review. Fix the exposure first.

---

### 7. Roadmap Mirror Sync

```bash
# Check for drift between wiki roadmap and codebase state
python3 scripts/game/sync_sunday_invest_moon_roadmap.py

# Apply sync if no unintended drift
python3 scripts/game/sync_sunday_invest_moon_roadmap.py --sync
```

**Expected**: No drift. The wiki roadmap (`wiki/synthesis/sunday-invest-moon-roadmap.md`) must accurately reflect completed phases 16.1–16.10. If drift exists, resolve manually before `--sync`.

---

### 8. PixelLab Balance Echo

```bash
python3 scripts/game/pixellab_gen.py balance
```

**Expected**: Prints current PixelLab API credit balance. Record this number in the session log — it is informational, not a pass/fail gate. Use it to decide whether to regenerate any unfinished assets before handoff.

---

### 9. Phase 16 Smoke Matrix

Run the Phase 16.9 Playwright suite OR complete the full manual smoke matrix:

```bash
# Option A: Playwright (requires dev server running at localhost:5173)
npm --prefix pixel-wealth-quest run dev &
npx playwright test pixel-wealth-quest/scripts/smoke-16.9.ts --headed

# Option B: Manual matrix
# Open http://localhost:5173 in browser, work through all rows in 16.9 matrix
# on both desktop 1280×720 and mobile 390×844 viewports
```

**Expected**:
- All manual smoke matrix rows: Pass
- `title → room transition` Playwright test: Pass
- `console has zero errors` Playwright test: Pass
- Net worth visibly changes (real number) after sleep
- Festival banner visible on festival day only
- Market-lady NPC at town stall at in-game noon

Save screenshots to `pixel-wealth-quest/docs/smoke-screenshots/16.10/`.

---

### 10. A-Wiki Preflight

```bash
/usr/local/bin/python3 scripts/agent-preflight.py
/usr/local/bin/python3 -m pytest tests/ -q
```

**Expected**: Both green. This confirms hook integrity, symlinks, index health, and no regressions in the A-Wiki agent layer caused by game-adjacent scripts.

---

## Evidence Bundle Checklist

After all 10 commands pass, assemble the evidence bundle:

- [x] Test run output captured: product vitest 679/679, 116 files
- [x] Typecheck: 0 errors confirmed
- [x] Build: `dist/` exists, no errors; Phaser chunk warning only
- [x] Feed boundary: 0 violations confirmed via npm script and direct node command
- [x] React Doctor: 100/100 score captured
- [x] Privacy scan: 0 findings in tracked A-Wiki files
- [x] Roadmap sync: no drift
- [x] PixelLab balance recorded: USD 2.74079388888015
- [x] Smoke matrix: 16.9 automated 12-test matrix included in full vitest run
- [x] A-Wiki preflight: green except expected dirty-tree warning before commit; pytest 356 passed / 9 skipped

---

## Done-When

- All 10 preflight commands return green (or expected output per spec above)
- Smoke matrix sign-off recorded through automated 16.9 matrix tests; screenshots remain optional unless a manual demo package is being assembled
- Roadmap entry for Phase 16.10 marked `[x]` in `wiki/synthesis/sunday-invest-moon-roadmap.md`
- `RESUME HERE` marker moved to Phase 17.1 backlog audit (Phase 16 is complete)
- Session completion summary recorded in the roadmap session log

---

## Final Commit Message Template

```
release(sunday-invest-moon): Phase 16 complete — economy real, guided loop, preflight green

- All 10 preflight checks green (679/679 tests, types clean, build OK)
- Feed boundary: 0 violations
- Smoke matrix: all rows pass, desktop + mobile
- Festival banner wired, NPC noon fix applied, debt portal live
- React Doctor 100/100, privacy scan 0 new findings
- Roadmap synced, Phase 16.10 complete

https://claude.ai/code/session_01HB2obs5rMScEzLugr9CjrP
```

---

## Post-Release Wiki Updates (required after commit)

1. **`wiki/synthesis/sunday-invest-moon-roadmap.md`** — Add session log entry:
   ```markdown
   ## Session Log
   - [YYYY-MM-DD] Phase 16 complete. Economy real, guided loop discoverable, preflight green.
     Test count: [N]. PixelLab balance: [N credits].
   ```

2. **`wiki/context/wiki-overview.md`** — Update the `sunday-invest-moon-roadmap` abstract to reflect Phase 16 completion status.

3. **`wiki/context/session-memory.md`** — Record completion decision: Phase 16 done, next priority is [Phase 17 / handoff / demo].

---

*Phase 16 is the last economy + guided-loop phase. After this preflight clears, the game is demo-ready.*
