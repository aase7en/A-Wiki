# A-Wiki optional dependencies — NOT installed by default.
# Install with: pip install -r requirements-optional.txt
#
# Each package is opt-in for a specific use-case. See the linked wiki page
# for when to enable it.

# turbovec — 16x-compression vector index. Opt-in alternative to sqlite-vec.
# Enable when wiki >5k entries. Default backend (sqlite-vec, in requirements.txt)
# stays untouched. See: wiki/entities/ai-tools/turbovec.md
turbovec>=0.1.0

# openmed — Healthcare AI: medical NER, PII detection, clinical de-identification.
# Required for pharmacy domain source processing (drug/disease entity extraction).
# See: wiki/entities/ai-tools/openmed.md
# Install backends:
#   pip install "openmed[hf]"         — standard (CPU/GPU)
#   pip install "openmed[mlx]"        — Apple Silicon (faster)
#   pip install "openmed[hf,service]" — + REST API on port 8770
openmed[hf]>=1.5.0

# scraper — document conversion pipeline. Install via SCRAPER_INSTALL=1.
# See: scripts/wiki/ingest-source.py (optional MarkItDown conversion)
markitdown>=0.1.0

# scraper — TLS-fingerprint HTTP fetch (bypass Cloudflare/Akamai).
# Binary alternative: brew install curl-impersonate-chrome (macOS)
# See: scripts/wiki/ingest-source.py (curl-impersonate fallback)
curl_cffi>=0.7.0

# Monte Carlo / quant analysis — monte-carlo-quant-analysis skill.
# Required to execute the runnable notebook at
# skills/awiki/monte-carlo-quant-analysis/examples/portfolio_mc_1yr.ipynb
# (simulate portfolio returns, compute VaR/CVaR/RRR, convergence diagnostics).
# See: skills/awiki/monte-carlo-quant-analysis/SKILL.md
numpy>=1.21
matplotlib>=3.4
scipy>=1.7              # scipy.stats.qmc (Sobol/Halton Quasi-MC), scipy.stats.norm (Importance Sampling)
nbconvert>=7.0          # jupyter nbconvert --execute (re-run notebook with outputs)
ipykernel>=6.0          # kernel for nbconvert execution
